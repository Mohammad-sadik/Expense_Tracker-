{
    "name": "bellcorp-expense-tracker",
    "version": "1.0.0",
    "description": "Bellcorp Personal Expense Tracker",
    "main": "server/src/server.js",
    "scripts": {
        "start": "node server/src/server.js",
        "build": "npm run install-server && cd client && npm install && npm run build",
        "install-server": "cd server && npm install",
        "install-all": "npm run install-server && cd client && npm install",
        "postinstall": "npm run build",
        "heroku-postbuild": "npm run install-server && npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "dotenv": "^16.0.0"
    },
    "engines": {
        "node": "20.x"
    }
}